// The backend server's address. Change this one line if you deploy the
// backend somewhere else later (e.g. Render, Railway) — every page that
// imports API_BASE_URL will automatically use the new address.
export const API_BASE_URL = "http://localhost:5000/api";